spring:
  profiles:
    active: test

  jpa:
    properties:
      hibernate:
        format_sql: true

file:
  dir: /home/ubuntu/backend/

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace
